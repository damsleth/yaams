# Autoresearch Idea Ledger

Append-only memory of retrieval experiments across the campaign. A fresh agent
reads this every iteration to (a) avoid re-trying discarded ideas and (b) pick
the next untried lever. `results.tsv` holds the numbers; this holds the *why*.

Format per line: `STATUS | idea | fitness_delta | commit | note`
STATUS ∈ `untried` `kept` `discarded` `crashed` `parked`. New ideas append at the
bottom of **Backlog**; once run, move the line to **Tried** with its verdict.

---

## Backlog (untried — pick the top-value one)

- untried | recall: why do common-term golds ("deployment", "damsleth") land rank=None — is per-index-k too small, or vec/FTS fusion dropping them? | — | — | ANSWERED jun20 review: NOT pure recall for most. `damsleth` IS pool-cutoff (vec#79, needs k>=100). But `nocos`/`møte` are IN-pool, starved by RRF (see single_modality_depth_credit). And `Nina aksjon korpskveld`/`Emilie driving school` are UNREACHABLE — the gold doc contains none of the query terms and is absent from vector top-500 → bad/unwinnable LABELS, quarantine them, don't tune for them.
- discarded | consolidation_boost_coverage_gated: in _fuse(), apply cfg.consolidation_boost to a consolidation's contribution ONLY for the index where it actually ranks well (fts_rank<=5 or vector_rank<=5 respectively); skip the boost on the weak-ranking index contribution | delta=-0.0106 | round 4 jun20. 1 regression; gate did not recover factual golds. DO NOT REVISIT.
- untried | synthesis_cons_near_tie_tiebreak: the prefer_consolidations counterpart — in _hydrate, ONLY when cfg.prefer_consolidations (synthesis shape), stable adjacent-swap to promote a consolidation B above an atomic item A when abs(A.rrf_score-B.rrf_score) <= EPS*A.rrf_score; sweep EPS in [0.02,0.04] | — | — | DIAGNOSIS: `russ` (shape=synthesis) gold imessage cons sits rank3 (0.0339) behind a teams_channels_brkh ATOMIC item 'Russetiden 2026' (0.0515) and an imessage cons; synthesis intent wants the consolidated session, and the human correction confirms it. Mirror of atomic_over_cons_near_tie_tiebreak but shape-scoped to synthesis only, so the two are non-overlapping by the prefer_consolidations gate and could even ship together. ORDERING-only and synthesis-gated => near-zero risk to factual golds. Note `russ` is a small-N synthesis case; if synthesis golds are too few to move the dev scalar, PARK rather than discard. Risk: minimal; verify regressions==0.
- untried | fts_rank_dominance_tiebreak: in _hydrate, after the rrf_score sort, a single stable adjacent-swap pass that promotes a lower candidate B above A when BOTH (a) B.fts_rank is not None and A.fts_rank is not None and B.fts_rank strictly < A.fts_rank (B is the more lexically relevant by FTS), AND (b) the rrf gap is within a generous band abs(A.rrf-B.rrf) <= EPS*A.rrf with EPS swept in [0.05,0.10]. ORDERING-only, pairwise relative (not absolute-threshold) — distinct from min_rank_near_tie_tiebreak (discarded) which used min(fts,vec) and the discarded single-index absolute-rank credits. | — | — | DIAGNOSIS: `deployment` golds — gold tier2 item ranks FTS=4 vs the winning teams_une item FTS=6, i.e. the gold is STRICTLY more lexically relevant, but loses on dual-coverage RRF sum (winner has vec=10, gold has vec=None). A pairwise FTS-dominance signal that absolute-threshold credit (single_index_high_rank_credit, fts_only_tier2_recovery, both discarded) cannot express. Risk: deployment's rrf gap is ~80% (0.0286 vs 0.0514) so EPS<=0.10 likely WON'T catch it — this idea targets the SMALLER-gap FTS-dominance cases in the tail (e.g. tier2 golds at rrf parity); if no gold sits inside the band, expect delta=0 and DISCARD. Worth one cheap run because it is pure ordering and zero-risk to recall. PARK if it can't reach any gold.
- untried | cons_breadth_mass_normalize: in _fuse(), divide a consolidation's accumulated rrf contribution by a mild breadth factor before the agreement pass — multiply the cons contribution by `1.0 / (1.0 + LAMBDA*log1p(item_count))` where item_count is read from the consolidations row (already SELECTed in _hydrate_consolidation; thread it into _fuse via the ranked-list tuple or a side lookup keyed on cid), sweep LAMBDA in [0.05,0.10,0.15]. | — | — | DIAGNOSIS: a sprawling 40-item session cons accrues a high FTS/vector rank simply because SOME of its many items co-mention the query terms — pure surface area, not focused relevance. In `troika medlemssak` and `medlemssak` the winning teams_brkh / imessage CONS are broad multi-topic sessions out-massing the atomic gold that is precisely on-topic. consolidation_boost_once and co_coverage_credit (both discarded, delta=0) addressed dual-coverage compounding but NOT cons size; this is an orthogonal lever — it deflates BREADTH, not coverage. Different from consolidation_boost_shape_gated (backlog, gates on query shape) and consolidation_boost_coverage_gated (backlog, gates on rank quality) — this scales by the cons's own item_count regardless of shape or rank. Risk: penalizes a legitimately large synthesis session that IS the right answer (`russ`) — keep LAMBDA small (log1p damps it) and verify synthesis recall@10 and regressions==0; if synthesis golds drop, gate the normalize OUT when cfg.prefer_consolidations.
- untried | bm25_margin_tiebreak: in _hydrate, after the rrf_score sort, ordering-only stable adjacent-swap promoting B above A when BOTH have fts_score set AND B.fts_score is materially stronger (more negative bm25; B.fts_score <= A.fts_score - MARGIN, abs threshold) AND the rrf gap abs(A.rrf-B.rrf) <= EPS*A.rrf; sweep MARGIN over bm25 units [0.5,1.0] and EPS in [0.05,0.08]. | — | — | DIAGNOSIS: bm25_magnitude_blend (discarded — only +0.0011 on clean code, cut by simplicity) tried to fold bm25 magnitude INTO the score; this instead uses the magnitude GAP purely as an ordering tiebreaker among near-tied rrf neighbors, adding zero to any score (so it cannot inflate non-gold candidates the way the blend did) and is distinct from fts_rank_dominance_tiebreak (backlog) which compares RANK not magnitude — two FTS hits at adjacent ranks can have very different bm25 confidence the rank ignores. Targets factual golds whose exact lexical match is markedly more confident than a near-tied competitor's weak partial match. Risk: bm25 magnitudes vary by query length/field weights so the abs MARGIN may be miscalibrated — sweep conservatively; ORDERING-only so zero recall risk. PARK if delta=0.
- untried | vector_distance_margin_tiebreak: in _hydrate, after the rrf_score sort, ordering-only stable adjacent-swap promoting B above A when BOTH have vector_distance set AND B.vector_distance <= A.vector_distance - MARGIN (B is materially closer in embedding space) AND the rrf gap abs(A.rrf-B.rrf) <= EPS*A.rrf; sweep MARGIN over cosine-distance units [0.03,0.05,0.08] and EPS in [0.05,0.08]. | — | — | DIAGNOSIS jun21 correction replay: the vector-space mirror of the backlog bm25_margin_tiebreak (FTS magnitude) — same shape, orthogonal modality. `kunde` gold is a signal item with vec=2 (very close) losing rank to a cons with vec=19; `Jan Guttormsen` gold is notes-only vec=12 vs near-tied competitors whose vector hit is much weaker. RRF buckets both as "has a vector rank" but discards the DISTANCE confidence; two adjacent rrf neighbors can have very different embedding proximity. DISTINCT from discarded vector_distance_gap_demote (round 3) which DEMOTED far vector-only candidates via a multiplicative penalty on score (changed magnitudes, delta=0) — this is a pairwise ORDERING swap that adds nothing to any score, so it cannot deflate a legitimate result, and it PROMOTES the closer hit rather than punishing the far one. Also distinct from single_modality_depth_credit (discarded, fragile additive credit on rank not distance). Risk: vector_distance is raw L2/cosine from sqlite-vec and its scale varies with the embedding model — calibrate MARGIN against observed best_dist spread before trusting; ORDERING-only ⇒ zero recall risk. PARK if no gold sits inside the band.
- untried | sole_fts_exact_over_vec_only_tiebreak: in _hydrate, after the rrf_score sort, ordering-only stable adjacent-swap that promotes a candidate B above the immediately-preceding A when B has a SOLE-INDEX EXACT lexical hit (B.fts_rank is not None and B.fts_rank <= 1 and B.vector_rank is None) AND A is VECTOR-ONLY (A.fts_rank is None and A.vector_rank is not None) AND the rrf gap abs(A.rrf-B.rrf) <= EPS*A.rrf; sweep EPS in [0.02,0.04,0.06]. | — | — | DIAGNOSIS jun21 correction replay: a tight cluster of factual golds where the gold is the SINGLE best lexical match (fts_rank 0-1, atomic ic=1) but vector-absent, sitting just under a vector-only consolidation that contributes no lexical signal at all — `27 april ... aktivitet` gold (notes fts=0 vec=None) at rank just below a cons (fts=None vec=0), gap 3.2%; `medlemssak` gold (atomic fts buried-but-present) under cons:675050e86, gap 1.5%. These competitors are DISJOINT in coverage (one purely lexical, one purely semantic) so neither modality dominates — RRF's single contribution each leaves them at near-parity and the cons edges ahead. DISTINCT from discarded lexical_top_hit_step_credit (additive _fuse credit for fts==0, over-promoted chatty items) and single_index_high_rank_credit (additive credit, fts<=2 OR vec<=2) — this is ORDERING-only, requires the COMPETITOR to be the complementary sole-modality (vector-only), and only fires inside a tight band, so it cannot inflate the broad over-promotion those two suffered. Also distinct from the backlog fts_rank_dominance_tiebreak (requires BOTH to have fts_rank) and bm25_margin_tiebreak (requires BOTH fts_score) — here the competitor has fts_rank=None by construction. Targets the exact "lexical-exact atomic vs semantic-only cons at parity" inversion. Risk: keep EPS tight (golds are at 1.5-3.2% gap) so it doesn't disturb the wider tail; ORDERING-only ⇒ zero recall risk. PARK if delta=0.
## Tried

- kept | thread_coherence_credit: in _hydrate, after the initial sort, additive RRF credit OMEGA * top3_cons_rrf_score for an atomic item whose thread_id equals the thread_id of a top-3 consolidation, gated to fts_rank is not None. OMEGA=0.15. | delta=+0.0328 | round 5. WIN (q 0.5816->0.6144, regressions=0, recall@10=0.9242). Structural thread link lifts the focused atomic member item once its parent session cons is already trusted. DO NOT REVISIT (won and kept).

- kept | tier2_boost_fused_order_cap: in _hydrate, AFTER the final rrf_score sort (else branch), single stable adjacent-swap pass that DEMOTES a tier2 item A below the next candidate B when B is non-tier2 AND B's pre-boost base rrf (comp.rrf_score) is strictly greater than A's pre-boost base rrf (a.score/cfg.tier2_boost). Guard: skip when A.vector_rank is None AND query_shape=="factual" (tier2_factual_coverage_recovery beneficiaries). Ordering-only, no score mutation. | delta=+0.0106 | round 6. WIN (q 0.6144->0.6250, regressions=0, hit_rate=0.6818, mrr_partial=0.4924, p95=172.0). Caps blunt tier2_boost blast radius without touching its magnitude. Protected existing kept tier2 golds (Identitet-Familie / did-Production-Deployment / Indices-Contract) via the vector_rank-is-None factual guard. DO NOT REVISIT (won and kept).

- discarded | dual_coverage_disagreement_demote: in _hydrate, after the rrf_score sort, a single stable adjacent-swap pass that demotes candidate A below the next candidate B when A is SINGLE-index (exactly one of A.fts_rank / A.vector_rank is None) AND B is DUAL-coverage (both ranks present) AND abs(A.rrf-B.rrf) <= EPS*A.rrf, sweep EPS in [0.03,0.06]. Guard: skip the demote when A.source==cfg.tier2_source. | delta=0 | — | campaign round 3. Quality unchanged (0.5816, delta=0, regressions=0). Near-tie adjacent-swap demoting single-index below dual-coverage did not improve gold ranking; pairwise relative signal did not fire on enough near-tie pairs to matter. DO NOT REVISIT.

- kept | tier2_factual_coverage_recovery: in _hydrate_item, additive RRF credit GAMMA/(rrf_k+fts_rank+1) for tier2 items where source==tier2_source AND query_shape=="factual" AND fts_rank<=5 AND vector_rank is None; BEFORE tier2_boost. GAMMA=0.5; query_shape forwarded via route.py cfg.query_shape. | delta=+0.0487 | — | autoresearch campaign round 2 (jun21). WIN (q 0.5329->0.5816, regressions=0, recall@10=0.9242). Recovers tier2 curated facts (Identitet - Familie, did v0.19 Production Deployment, Indices Contract) that are FTS-present/vector-absent and lose the dual-coverage RRF race. The vector_rank-is-None gate contained blast radius — no non-tier2 factual regressions. DO NOT REVISIT (won and kept).

- discarded | graded_rank_agreement: in _fuse() post-loop pass, replace the single binary `<=2 both` multiplier with a GRADED schedule: TIER1 multiplier (swept [0.08,0.10]) when both ranks <=1, the existing 0.05 when both <=2, and TIER3 (swept [0.02,0.03]) when both <=4 | delta=0 | — | campaign round 1 (jun20 campaign). Quality unchanged (0.5329, delta=0, regressions=0). Graded curve did not catch additional golds vs the flat step; the <=2 step is already at the optimal granularity for this corpus. DO NOT REVISIT.

- discarded | single_modality_depth_credit: in _fuse(), additive GAMMA/(rrf_k+rank+1) for docs covered by exactly ONE index ranking within DEPTH_FRAC*per_index_k of the pool top; gated to single-modality (other rank None). Swept GAMMA[0.3..2.0] x DEPTH_FRAC[0.25..0.75]. | best clean +0.0013 dev (0.5539->0.5552), +0.0012 all; γ=0.5 gave +0.039 but 1 regr | reverted | jun20 review (re-anchored eval-fixed harness, anchor tag reanchor-jun20). DIRECTION CONFIRMED but MARGINAL+FRAGILE. The diagnosed buried golds nocos(vec#13)/møte(vec#36) are NOT recovered at any gate-passing GAMMA — credit strong enough to lift them (γ=0.5) regresses another gold and drops recall@10. Landscape is NON-MONOTONIC on the 58-q dev set (γ 0.35→0.4→0.45→0.5 = 0.5552→0.5508→0.5499→0.5931), i.e. knife-edge tie-break flips not a smooth signal. Clean γ=0.35 moves only 2 corrections (Helene NOCOS 13→11 still out of top10, Jan Guttormsen 4→3), test split neutral. +0.0013 for an 11-line hot-path branch is below the project's own noise bar (cf. bm25_magnitude_blend discarded at +0.0011). REAL BLOCKER is the gold set (too small/sparse to extract a robust single-modality signal) + the buried golds being NER-term-absent — not the fusion algorithm. Revisit only with denser correction labels OR a recall-reweighted fitness.
- discarded | per_index_k 50->75/100/200 (candidate pool size) | dev quality 0.5539->0.5499 (DOWN), recall@10 0.914->0.931 (UP) | — | jun20 review (eval-fixed harness). NOT a free win — TRADES quality for recall. rank1 holds at 40 (the swap is rank-1-neutral) but mrr_partial drops (0.438->0.425 dev) because the bigger pool injects distractors above correction golds. k=75 is the recall ceiling (same 0.897 as k=200) with 0 regressions, but FAILS the keep-gate (no quality gain). k>=100 also trips a rank-1 regression. CONFIRMS the old "k=50 beat k=80" finding. The recall headroom is real but the fix is NOT a bigger pool — it's smarter fusion so recovered golds rank well once recalled (see single_modality_depth_credit). DO NOT bump per_index_k unless the fitness is reweighted toward recall.
- discarded | eval at user's stored top_k (HARNESS BUG, fixed 235750b) | recall@10 0.853->0.882, mrr_partial 0.401->0.414, all-split quality 0.532->0.536 | 235750b | jun20 review. The harness replayed each gold at its *stored* top_k (1..80) and truncated before measuring rank — `what is the budget` (gold FTS#1, stored k=1) and `AkershusRK funn` (gold FTS#4, stored k=3) scored as total misses. Now replays at fixed _EVAL_TOP_K=50. Pure measurement fix, no retrieve/ change. ANCHOR in .autoresearch_state.json predates this and is non-comparable — re-establish before next round.

- discarded | agreement_band_widen_resweep: in _fuse(), resweep rank_agreement_multiplier params jointly — DELTA in [0.05,0.07,0.10] × THRESH in [2,3] | delta=0 | — | campaign round 2. Quality unchanged (0.5508, delta=0, regressions=0). No grid point improved on shipped defaults (DELTA=0.05, THRESH=2); wider band and higher DELTA did not catch additional golds. DO NOT REVISIT.
- discarded | consolidation_boost_shape_gated: in _fuse(), apply the cfg.consolidation_boost multiplier to a cons contribution ONLY when the parsed shape is synthesis | delta=0 | — | campaign round 1. Quality unchanged (0.5508, delta=0, regressions=0). Shape-gated consolidation boost did not improve gold ranking; prefer_consolidations defaults to True so the boost already fires everywhere, but gating it to synthesis shape alone yielded no measurable gain on the dev set. DO NOT REVISIT.
- discarded | factual_tier2_boost_lexgated: in _hydrate, raise tier2_boost for factual-shape queries ONLY when the candidate item lexically matched the query (comp.fts_rank is not None and comp.fts_rank <= 3 for tier2_ledger items), applying cfg.tier2_boost -> max(cfg.tier2_boost, 1.5) under that gate | delta=0 | — | campaign round 7. Quality unchanged (0.5508, delta=0, regressions=0). Narrow lex-gated tier2 boost did not improve gold ranking; prerequisite lexical_top_hit_step_credit (round 6) confirmed step-credit under-fires for tier2 specifically, but gated multiplier also yields no gain. DO NOT REVISIT.
- discarded | subject_exact_match_credit: in _hydrate_item, when ALL query topic tokens appear (casefolded substring) in row['subject'], add additive rrf credit BETA*comp.rrf_score, sweep BETA in [0.05,0.10] | delta=0 | — | campaign round 8. Quality unchanged (0.5508, delta=0, regressions=0). Strict all-tokens subject gate did not improve gold ranking; factual golds sitting at rank 2-3 behind longer-body items were not lifted by the subject credit. DO NOT REVISIT.
- discarded | min_rank_near_tie_tiebreak: in _hydrate, after sorting by rrf_score, stable bubble pass swaps adjacent A,B when abs(A.rrf_score-B.rrf_score) <= EPS*A.rrf_score AND min(B.fts_rank,B.vector_rank) strictly better than min(A.fts_rank,A.vector_rank); sweep EPS in [0.01,0.02,0.03] | delta=0 | — | campaign round 6. Quality unchanged (0.5508, delta=0, regressions=0). Min-rank ordering swap did not improve gold rank. DO NOT REVISIT.
- discarded | atomic_over_cons_near_tie_tiebreak: in _hydrate, stable adjacent-swap pass — when an atomic item B sits directly below a consolidation A and abs(A.rrf_score-B.rrf_score) <= EPS*A.rrf_score, promote the atomic item above the cons; sweep EPS in [0.02,0.04]; gate out when cfg.prefer_consolidations is True | delta=0 | — | campaign round 5. Quality unchanged (0.5508, delta=0, regressions=0). Near-tie ordering swap did not improve gold rank. DO NOT REVISIT.
- kept | rank_agreement_multiplier: in _fuse(), post-loop pass multiply rrf_score by (1 + 0.05) when BOTH comp.fts_rank<=2 AND comp.vector_rank<=2 (mutual top-of-both agreement) | delta=+0.0045 | — | campaign round 4. WIN (q 0.5463->0.5508, regressions=0). Strongest dual-modality agreement signal so far. DELTA=0.05 applied.
- discarded | rrf_k_split_fts_vec: give FTS and vector their own rrf_k in _fuse (use cfg.rrf_k for FTS, a separate vec_rrf_k for vector contributions), sweep vec_rrf_k in [20,40,60] holding FTS at 30 | delta=-0.0028 | — | campaign round 2. Quality dropped (0.5463→0.5435); per-index rrf_k split did not improve ranking — FTS and vector benefit from the same k value. DO NOT REVISIT.
- discarded | vector_distance_gap_demote: in _fuse()/_hydrate, demote vector-only candidates (fts_rank is None) whose vector_distance is far from the best vector hit — multiply rrf_score by clamp(1 - LAMBDA*(dist - best_dist)/best_dist, FLOOR, 1.0), compute best_dist as min vector_distance across fused, sweep LAMBDA in [0.3,0.5] FLOOR=0.7 | delta=0 | — | campaign round 3. Quality unchanged (0.5463, delta=0); vector-distance gap demoting did not move scores — tangential-semantic tail did not contribute measurable harm to gold ranking at tested lambdas. DO NOT REVISIT.
- discarded | fts_only_tier2_recovery: in _hydrate, for items where source==tier2_source AND comp.vector_rank is None AND comp.fts_rank is not None AND fts_rank<=1, add an ADDITIVE rrf credit equal to one extra index contribution 1/(cfg.rrf_k+fts_rank+1) before applying tier2_boost | delta=0 | — | campaign round 1. Quality unchanged (0.5463, delta=0); structural gap credit for FTS-only tier2 golds yielded no measurable improvement. DO NOT REVISIT.
- discarded | occurrence_relevance_floor_sweep: sweep OCCURRENCE_RELEVANCE_FLOOR / generalize relevance_floor to factual shape; tried small floor (0.05-0.1) on factual to prune weak-tangential tail, and swept occurrence floor [0.15,0.25,0.3] | -0.0054 | — | jun20 round 8. Quality dropped; floor tightening did not separate gold from near-ties. DO NOT REVISIT.
- discarded | single_index_high_rank_credit: in _fuse(), when a doc is covered by exactly ONE index but ranks very high there (fts_rank<=2 OR vector_rank<=2), add a small additive RRF credit ~+1/(rrf_k+rank+1)*GAMMA, sweep GAMMA∈[0.15,0.3,0.5] | -0.0054 dev (0.5517→0.5463) | — | jun20 round 7. Quality dropped; crediting confident sole-index hits upward did not close the structural gap to dual-coverage results. DO NOT REVISIT.
- discarded | lexical_top_hit_step_credit: additive RRF step bonus in _fuse() for the FTS rank-0 entry only (the single best lexical match), contribution += BETA*base_contribution when fts_rank==0, sweep BETA∈[0.10,0.20,0.30] | -0.0266 dev (0.5517→0.5251) | — | jun20 round 6. Regression (1) and quality drop; step credit over-promoted chatty exact-token items over correct semantic-only results. DO NOT REVISIT.
- discarded | bm25_magnitude_blend: blend normalized bm25 magnitude into FTS RRF contribution in _fuse() | campaign measured +0.0054 (0.5517) but that was a STALE-HEAD artifact; on clean code it's only +0.0011 (0.5463→0.5474) | reverted | jun20. Marginal/noise gain for 29 lines added to _fuse — discard by the simplicity criterion. The inflated 0.5517 came from the HEAD-staleness bug (fixed). DO NOT REVISIT at this alpha.
- discarded | sender_participant_rrf_credit: additive RRF bonus in _hydrate for items/cons whose sender or participants match query name tokens, scoped to person-lookup queries | -0.0043 dev (0.5517→0.5474) | — | jun20 round 5. Regression; person-as-participant signal did not improve gold ranking despite targeted scope. DO NOT REVISIT.
- discarded | factual_tier2_boost: raise tier2_boost for factual shape in route.py (cfg.tier2_boost = max(cfg.tier2_boost, 1.6) when parsed.shape=="factual") | -0.0007 dev (0.5463→0.5456) | — | jun20 round 3. Slight regression; tier2 over-promotion hurts factual golds whose gold is NOT tier2. DO NOT REVISIT.
- discarded | consolidation_boost_once: apply consolidation_boost to fused cons score once after RRF loop, not per-contribution inside loop | +0.0000 dev (0.5463→0.5463) | — | jun20 round 1. No quality gain; delta=0. Dual-coverage compounding hypothesis did not yield measurable improvement. DO NOT REVISIT.
- discarded | co_coverage_credit: single-index penalty / co-coverage credit in _fuse() — scale rrf_score by f<1 when only one index covers the item (sweep f∈[0.85,0.9,0.95]) | +0.0000 dev (0.5463→0.5463) | — | jun20 round 2. No quality gain; delta=0. RRF already captures the dual-coverage advantage via summed contributions; an explicit scaling factor adds no signal. DO NOT REVISIT.
- kept | per_index_k_resweep: DEFAULT_PER_INDEX_K 80→50 + consolidation_boost 1.1→1.05 | +0.0057 dev (0.5406→0.5463) | — | jun19 round 3. Re-validated on 68-gold set; k=50 outperforms k=80 on the denser set.
- kept | consolidation_boost_resweep: consolidation_boost 1.1→1.05 | +0.0093 dev (0.5313→0.5406) | — | jun19. Temporal regression no longer applies after narrow-date de-boost was gated separately; global knob moved freely and yielded quality gain.
- discarded | RRF_K re-sweep on denser gold set | -0.0093 (3 runs, 1 had a regression) | campaign-jun19 | RRF_K=30 confirmed still optimal on the 68-gold set; every off-30 value lost quality. DO NOT REVISIT.
- parked | event_anchored narrow-window cons de-boost | noisy: -0.0027 then +0.0096 across two runs | campaign-jun19 | within noise on the dev set; +0.0096 doesn't justify the added branch by the simplicity criterion. Revisit only if event-anchored golds grow.

- kept | browse-window fallback for empty time-windowed queries | +0 (fixes zero-result misses, not scored) | b192ca3 | jun19. Fires only on empty results, can't regress.
- kept | entity boost_factor 1.5→3.0 | +0.0047 dev | 74deedd | jun19. Saturates at 3.0 (identical 4/5/8).
- kept | narrow-date (≤3d) consolidation de-boost 0.85 | +0.043 dev (0.4878→0.5313) | 6e3b8a5 | jun19. Window-width gate is essential — flat de-boost regressed the month-range hit.
- discarded | per_field_bm25_sender_weight: sender weight currently 1.0 — lowering it did not reduce false matches on chatty senders | -0.0128 | — | round 2 jun19. FTS_ITEM_WEIGHTS in hybrid.py. DO NOT REVISIT.
- discarded | phrase / NEAR(...,N) clause in _fts_query | +0.000 (neutral) | — | jun19. bm25 over OR-tokens already ranks co-occurrence high; redundant. DO NOT REVISIT.
- parked | subject-token rerank | promising hit_rate but 1 regression | — | revisit only if the regression policy changes to net-neutral.
- parked | large tier2_boost changes | regressed several golds | — | jun8/10.
- discarded | recency decay (10% max penalty) | 4 regressions | — | jun10.
- discarded | vector-k decoupling | failed no-regression floor | — | jun10.
