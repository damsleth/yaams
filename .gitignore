.env
uv.lock
**.DS_Store
.claude/
.plans/
.tmp/
.pytest_cache/
.venv/
__pycache__/
*.pyc
*.egg-info/
*.db
*.db-shm
*.db-wal
.serena/

# Local-only configuration with personal addresses, paths, and dictionary entries.
# Track config.yaml.example instead.
config.yaml

# Local data directories that may end up under the repo root.
# (Ingest sources at ~/Library/... live in $HOME and never enter the repo.)
data/
yaams-data/
*.mbox
*.emlx

# autoresearch harness machine-local rank cache (results.tsv is kept as the experiment log)
scripts/.autoresearch_state.json
